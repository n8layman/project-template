name: Process New Papers

on:
  push:
    paths:
      - 'resources/papers/**'
  workflow_dispatch:

jobs:
  process-papers:
    runs-on: ubuntu-latest
    container: rocker/tidyverse:4.3.1  # Use a specific Rocker R version

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install required R packages
        env:
          RENV_CONFIG_AUTO_RESTORE: "FALSE"  # Disable renv auto-restore
        run: |
          install.packages(c("pdftools", "httr", "stringr", "bibtex"), dependencies=TRUE)
        shell: Rscript {0}

      - name: Run R script
        env:
          RENV_CONFIG_AUTO_RESTORE: "FALSE"  # Disable renv auto-restore
        run: Rscript scripts/process_papers.R
        shell: Rscript {0}