# Saturday, February 8, 2025

#Accomplished

1. Successfully integrated foam with vscode.
2. Set up a keybinding for daily note `cmd-shift-d`
3. Raised an [issue](https://github.com/foambubble/foam/issues/1422) with foam's github site requesting the ability to restrict foam to a single folder in a repo.
4. Updated issue to include how to restrict foam to only '.md' files.
5. Merge changes from template to bat-interactions brachn using `git merge project-template/main --allow-unrelated-histories`
6. Figured out how to hide foam specific things in the vscode file explorer if needed. Right click on view in explorer to hide.
7. Figured out how to use tags so I can quickly tag people in meetings.
8. When using command-enter to run code in radian terminal function definitions get a `Error: unexpected '}' in "}" ` error. Solution is to add `"r.bracketedPaste": true,` to .vscode/settings.json

#2D

1. Create vignette about how this was all done.
2. Figure out how to run code chunks in generic markdown.

```{r setup, include=FALSE}
print("test")


```

